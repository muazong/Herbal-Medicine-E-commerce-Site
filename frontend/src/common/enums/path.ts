export enum PATH {
  HOME = '/',
  ABOUT = '/about',
  CONTACT = '/contact',
  PRODUCTS = '/products',
  CART = '/cart',
  ORDER = '/order',
  ORDERED = '/ordered',
  ORDERED_CANCELED = '/ordered/canceled',
  LOGIN = '/auth/login',
  REGISTER = '/auth/register',
  PROFILE = '/profile',
}
